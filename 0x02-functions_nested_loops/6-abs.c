#include "main.h"
/**
 * _abs - compute absolute value of an integer
 * @n: integer to becomputed
 *
 * Return: The ablsolute value of integer
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
